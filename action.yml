name: 'CMake Actions Workflows for HexaGen'
description: 'Reusable workflows for CMake builds and PR creation for HexaGen projects.'
author: 'JunaMeinhold'
branding:
  icon: 'package'
  color: 'blue'
runs:
  using: 'composite'
  steps: []
inputs: {}
outputs: {}

# This repository provides the following reusable workflows:
# - ./cmake.yml: Build and upload native artifacts using CMake
# - ./create-pr.yml: Download, trim, and create PRs with artifacts
#
# See the README.md for usage instructions.
